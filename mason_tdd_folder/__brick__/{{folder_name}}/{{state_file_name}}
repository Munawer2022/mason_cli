{{#isGet}}
import '/data/models/{{folder_name}}/{{folder_name}}_model.dart';
import '{{folder_name}}_initial_params.dart';
import '/config/response/api_response.dart';

class {{class_name}}State {
  final ApiResponse<{{class_name}}Model> response;

  {{class_name}}State({required this.response});
  factory {{class_name}}State.initial({required {{class_name}}InitialParams initialParams}) =>
      {{class_name}}State(response: ApiResponse.initial({{class_name}}Model.fromJson({})));
  {{class_name}}State copyWith(
          {ApiResponse<{{class_name}}Model>? response}) =>
      {{class_name}}State(
          response: response ?? this.response);
}
{{/isGet}}

{{#isPost}}
import '/data/models/{{folder_name}}/{{folder_name}}_success_model.dart';
import '{{folder_name}}_initial_params.dart';

class {{class_name}}State {
  final {{class_name}}SuccessModel success;
  final bool isLoading;
  final String? error;

  {{class_name}}State({required this.success, required this.isLoading, this.error});
  factory {{class_name}}State.initial({required {{class_name}}InitialParams initialParams}) =>
      {{class_name}}State(
          success: {{class_name}}SuccessModel.fromJson({}),
          isLoading: false);
  {{class_name}}State copyWith({
            {{class_name}}SuccessModel? success,
          bool? isLoading,
          String? error}) =>
      {{class_name}}State(
          success: success ?? this.success,
          isLoading: isLoading ?? this.isLoading,
          error: error ?? this.error);
}
{{/isPost}}
 {{#isNoThing}}
import '{{folder_name}}_initial_params.dart';

class {{class_name}}State {

  {{class_name}}State();
  factory {{class_name}}State.initial({required {{class_name}}InitialParams initialParams}) =>
      {{class_name}}State();
  {{class_name}}State copyWith() =>
      {{class_name}}State();
}
{{/isNoThing}}