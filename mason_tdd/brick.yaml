name: mason_tdd
description: Generate Flutter apps with Clean Architecture, Bloc state management, authentication, and production features.
repository: https://github.com/munawerdev/mason_cli

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+21

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  # mason: ">=0.1.0-dev.41 <0.1.0"
  # mason: ">=0.1.0-dev.60 <0.1.0"
  mason: ^0.1.0

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, or array)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
vars:
  name:
    type: string
    description: The name of the feature (used to generate the page, cubit, model, etc.)
    default: Dash
    prompt: What is the name of the feature?

  dio:
    type: enum
    description: Select the Api method to generate for the page
    values:
      - get
      - post
      - none
    prompt: Which Api method would you like to generate for the page?
